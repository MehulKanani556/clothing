// Layout component can be used for shared layout structure if needed
// Currently using Header and Footer directly in App.js

export default function Layout({ children }) {
  return (
    <>
      {children}
    </>
  );
}

